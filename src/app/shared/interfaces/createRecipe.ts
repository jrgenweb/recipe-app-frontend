/*export interface CreateRecipeStepDto {
  name: string;
  description: string;
  stepOrder: number;
}

export interface CreateRecipeIngredientDto {
  ingredientId: string;
  quantity: string; // 'g' | 'ml' | 'db' stb.
}

export interface CreateRecipeDto {
  name: string;
  description: string;
  categoryIds?: string[];
  steps?: CreateRecipeStepDto[];
  ingredients?: CreateRecipeIngredientDto[];
  imageUrls?: string[];
}
*/