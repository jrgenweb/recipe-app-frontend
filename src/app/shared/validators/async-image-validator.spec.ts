import { AsyncImageValidator } from './async-image-validator';

describe('AsyncImageValidator', () => {
  it('should create an instance', () => {
    expect(new AsyncImageValidator()).toBeTruthy();
  });
});
